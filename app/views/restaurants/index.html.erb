<h1>All Restaurants</h1>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= restaurant.name %>
    <%= link_to "Show more", restaurant %></li>
  <% end %>
</ul>

<%= link_to "Add new restaurant", restaurants_new_path %>
